<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Red Light Green Light Game</title>
  <style>
    body {
      margin: 0;
      background-color: red;
      height: 100vh;
      transition: background-color 0.1s ease;
      display: flex;
      align-items: center;
      justify-content: center;
      overflow: hidden;
    }

    #winnerText {
      display: none;
      font-size: 325%;
      font-family: sans-serif;
      color: white;
      text-align: center;
      line-height: 1;
      white-space: nowrap;
    }
  </style>
</head>
<body>

<div id="winnerText">We have a winner!!!</div>

<!-- Audio element for the Squid Game music -->
<audio id="redLightMusic" preload="auto">
  <source src="squid_game_red_light.mp3" type="audio/mpeg">
  Your browser does not support the audio element.
</audio>

<script>
  let cycleStarted = false;
  const winnerText = document.getElementById('winnerText');
  const redLightMusic = document.getElementById('redLightMusic');

  function startCycle() {
    const redDuration = Math.floor(Math.random() * 5000) + 2000;

    // RED light phase
    document.body.style.backgroundColor = 'red';
    winnerText.style.display = 'none';
    redLightMusic.currentTime = 0;
    redLightMusic.play();

    setTimeout(() => {
      // GREEN light phase
      document.body.style.backgroundColor = 'green';
      winnerText.style.display = 'none';
      redLightMusic.pause();
      redLightMusic.currentTime = 0;

      setTimeout(() => {
        startCycle();
      }, 9000); // green light duration

    }, redDuration);
  }

  document.addEventListener('keydown', function(event) {
    if (event.key === '1') {
      document.body.style.backgroundColor = 'blue';
      winnerText.style.display = 'block';
    } else if (event.key === '2' && !cycleStarted) {
      cycleStarted = true;
      startCycle();
    }
  });
</script>

</body>
</html>
